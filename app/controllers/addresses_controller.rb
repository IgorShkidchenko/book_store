class AddressesController < ApplicationController
  # def create
  #   adress = Addresses.new(addresses_params)
  #   adress.save ? flash[:success] = 'good' : flash[:danger] = review.errors.full_messages.to_sentence
  #   redirect_back fallback_location: root_path
  # end

  # private

  # def address_params
  #   params.require(:address).permit(:first_name, :last_name, :address, :city, :country, :zip, :phone)
  # end
end
