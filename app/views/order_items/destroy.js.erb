$('.cart_part').html("<%= j render 'layouts/header/cart', order: @order.decorate %>");
$('tr.<%= @order_item.id %>').fadeOut();
$('div.<%= @order_item.id %>').fadeOut();
$('div.<%= @order.id %>').replaceWith("<%= j render 'orders/order_info/order_summary', order: @order.decorate, right: true %>");
$('.flash').html("<%= j render partial: 'layouts/flash', locals: { danger: flash[:danger] } %>");

var items_quantity = '<%= @order.order_items.count %>';
var min_quantity = '<%= OrderItem::MINIMUN_QUANTITY %>';

if(items_quantity == min_quantity){
    location.reload();
}
